# docker/grafana/provisioning/datasources/datasource.yml
# Auto-provisioned on Grafana startup — no manual "Add data source" click.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
